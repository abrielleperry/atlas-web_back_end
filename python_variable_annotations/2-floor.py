#!/usr/bin/env python3
""" func floor takes a float n as argument and returns the floor of the float.
"""
import math


def floor(n: float) -> int:
    """ returns largest int less than or equal to n

    Args:
        n (float): input number to determine floor

    Returns:
        int: floor of the float
    """
    return math.floor(n)
